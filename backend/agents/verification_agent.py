"""
Verification Agent -- checks each AI tool in a manifest against the tool registry.
Implemented: BUILD_PLAN.md Days 9-11 (Aug 10-12)
"""


def verify_tool(tool_name: str) -> dict:
    """Returns {status: cleared|flagged|needs_review, evidence: str} for a given tool."""
    raise NotImplementedError("See BUILD_PLAN.md Days 9-11")
